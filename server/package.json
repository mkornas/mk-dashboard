{
  "name": "mk-dashboard-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Homelab server dashboard API: host metrics from /proc and /sys, containers from the Docker socket.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts",
    "test": "node --test 'test/**/*.test.ts'",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "@mk-kit/auth": "^0.2.0",
    "fastify": "^5.6.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/web-push": "^3.6.4",
    "jose": "^6.2.12",
    "typescript": "~6.0.2"
  }
}
